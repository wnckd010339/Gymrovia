# Gymrovia CSS 구조

- `foundation/`: 로그인·오류 화면에서 사용하는 최소 공통 스타일
- `layouts/`: 관리자 사이드바, 트레이너 사이드바, 회원 헤더 등 역할별 레이아웃
- `pages/<role>/`: 특정 화면에서만 불러오는 페이지 전용 스타일

스타일은 공통 기반(필요한 화면만), 역할 레이아웃, 페이지 전용 순서로 불러옵니다.
역할 레이아웃에서 페이지 CSS를 `@import`하지 않고 해당 Thymeleaf 화면이 `<link>`로 직접 불러옵니다.
